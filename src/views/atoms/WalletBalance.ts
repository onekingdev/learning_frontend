import styled from 'styled-components';

export const Balance = styled.p`
  font-size: 16px;
  font-weight: 600;
  font-family: Montserrat;
  position: absolute;
  margin: 0;
  right: 12px;
  bottom: 9px;
`;
