import {FC} from 'react';
import {ReactPortal} from 'react';
import {ReactDOM} from 'react';
import styled from 'styled-components';

export const CardCollectibleModal: FC = () => {
  return ReactDOM.createPortal(<div></div>);
};
